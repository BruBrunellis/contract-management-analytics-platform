[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
# Disable the persistent cache so Windows never needs to update .pytest_cache.
addopts = "-p no:cacheprovider"

[tool.ruff]
line-length = 100
target-version = "py312"
